import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  card: {
    justifyContent: "center",
    alignItems: "center",
    flex: 1,
    paddingBottom: 10
  },
  text: {
    fontFamily: "Denk One",
    fontSize: 24,
    color: "#FFFFFF",
  }
});
