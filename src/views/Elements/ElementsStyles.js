import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  card: {
    alignItems: "center",
    paddingBottom: 10
  },
  text: {
    fontFamily: "Denk One",
    fontSize: 24,
    color: "#FFFFFF",
  }
});
